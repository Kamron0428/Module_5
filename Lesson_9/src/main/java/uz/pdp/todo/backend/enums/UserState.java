package uz.pdp.todo.backend.enums;

public enum UserState {
    AWAITING_TITLE,
    AWAITING_DESCRIPTION,
    AWAITING_PRIORITY,
    AWAITING_CATEGORY,
    AWAITING_COMPLETE_ID,
    AWAITING_DELETE_ID
}

